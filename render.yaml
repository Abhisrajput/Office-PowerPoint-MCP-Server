services:
  - type: web
    name: office-ppt-mcp-server
    env: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      PORT: 8000
